type Props = {};

const ProfileAccount = (props: Props) => {
  return (
    <div>
      ProfileAccount here will be the account details form where users can
      update their name, email, phone number, and address. We will also include
      a section for changing the password and managing security settings. This
      page will allow users to keep their account information up to date and
      secure.
    </div>
  );
};

export default ProfileAccount;
