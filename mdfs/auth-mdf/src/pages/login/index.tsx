import LoginForm from "../../components/login-form";
import Layout from "../layout";
const LoginPage = () => {
  return (
    <Layout>
      <LoginForm />
    </Layout>
  );
};

export default LoginPage;
